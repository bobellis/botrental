<h1>User List</h1>

<% @users.each do |user|%>
<li><%= link_to user.email, user_path(user) %>
<%= link_to "Delete User", user_path(user), :method => :delete, :class => "btn btn-xs btn-danger"%> </li>
<% end %>
