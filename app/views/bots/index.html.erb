<h1>Bot List</h1>

<% @bots.each do |bot|%>
<li><%= link_to bot.name, bot_path(bot) %>
<%= link_to "Favorite", new_favorite_path(bot_id: bot.id) %></li>
<% end %>

<%= link_to "New Bot", new_bot_path %>
